import ReactHtmlParser from 'react-html-parser';
import { Link, Redirect } from "react-router-dom";

export const productmsg = {
	 product: {
    		productListEmpty:ReactHtmlParser("<div class='data-empty'>Sorry!! No Product to Display</div>")
    }
}